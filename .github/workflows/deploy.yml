name: Kubernetes Manifests CI

on:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: List Kubernetes manifests
        run: |
          echo "Kubernetes YAML files:"
          ls k8s

